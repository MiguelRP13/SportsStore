@model IEnumerable<Product>
@{
    ViewData["Title"] = "List";
}

<h2>Product List</h2>

@foreach(Product p in Model)
{
    <div>
        <h3>@p.Name</h3>
        <p>p.Description</p>
        <h4>@p.Price.ToString("c")</h4>
    </div>

}